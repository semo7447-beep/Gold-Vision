# الميزات المؤجَّلة — Gold Vision

جدول مرجعي بكل الميزات/القرارات اللي أجّلناها عمداً أثناء تطوير التطبيق، مع سبب التأجيل والشرط اللي لازم يتحقق قبل تفعيلها. الهدف: مرجع سريع بدل إعادة شرح كل مرة.

| # | الميزة | الحالة | الشرط/السبب | ملاحظات |
|---|--------|--------|-------------|----------|
| 1 | مقارنة أسعار المصنعية بين محلات الذهب (بيانات يُدخلها المستخدمون) | مؤجَّلة | لحد ما يوصل عدد المستخدمين ~50,000 | تحتاج كتلة بيانات كافية من البداية عشان تكون مفيدة فعلاً (مشكلة البيضة والدجاجة) |
| 2 | خادم وسيط (Cloud Functions) لجلب أسعار الذهب الحية — بدل الطلب المباشر من التطبيق لـ GoldAPI.io | **مبني جاهز، غير مفعَّل** | لحد ما يكبر عدد المستخدمين أو يتحقق دخل يبرر التكلفة | الكود موجود كامل بمجلد `functions/` (`refreshGoldPrice`, `getGoldPrice`, `refreshGoldHistory`, `getGoldHistory`) + `firebase.json` + `.firebaserc`. يحتاج تفعيل خطة Firebase Blaze المدفوعة — بالسعودية تحتاج فوترة عبر موزّع (CNTXT) يطلب بريد بزنس (غير Gmail) مو متوفر حالياً. هذا هو نفسه "الإجراء الإضافي" اللي تتخذه التطبيقات الكبيرة لحماية مفاتيح الـ API من الاستخراج عبر فك تجميع APK |
| 3 | رمز تحقق رقمي (OTP) بدل رابط تأكيد الإيميل عند التسجيل | مؤجَّلة | لحد مرحلة تحقيق دخل فعلي | تحتاج بنية تحتية مدفوعة (Cloud Functions + خدمة إرسال زي SendGrid) |
| 4 | نطاق مخصص (custom domain) + بريد بزنس خاص بالمشروع | مؤجَّلة | نفس شرط البند 2 (مطلوب لفتح حساب CNTXT للفوترة) | تكلفة تقريبية ~10-15 دولار/سنة للنطاق + Zoho Mail مجاني للبريد |
| 5 | تخصيص قالب بريد تأكيد الإيميل (اسم مرسل + نص احترافي كامل بعلامة Gold Vision) | **محظورة من فيربيس نفسه — مو مجرد تأجيل** | Firebase تمنع تعديل نص الرسالة (Message body) بالكامل على هذا المشروع، حتى تذكرة الدعم الرسمية فشل إرسالها | تم تغيير اسم المرسل والعنوان بنجاح (هذا الجزء يعمل)، لكن نص الرسالة نفسه مقفول بقيد منصّة Firebase، غالباً مرتبط بخطة Spark المجانية — لا حل حالياً من جهتنا |

## كيف تستخدم هذا الملف

اسأل عن أي بند بس بذكر رقمه أو اسمه ("وش وضع البند 2؟")، أو قول "حان وقت كذا" لما يتحقق الشرط وأبدأ التنفيذ.
