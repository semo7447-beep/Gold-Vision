package com.goldvision

// تذكير اجتماعات الفيدرالي غير مفعّل بعد على iOS — بلا تأثير حالياً
internal actual object FedMeetingNotificationScheduler {
    actual fun setEnabled(enabled: Boolean) {
    }
}
